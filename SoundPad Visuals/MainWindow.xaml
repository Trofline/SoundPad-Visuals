<Window x:Class="SoundPad_Visuals.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title=""
        Width="410"
        Height="338"

        AllowsTransparency="True"
        Background="Transparent"
        WindowStyle="None"
        Topmost="True"
        ShowInTaskbar="False"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        MouseRightButtonDown="Window_MouseRightButtonDown">

    <Window.Resources>
        <Style x:Key="FocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" StrokeDashArray="1 2" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" SnapsToDevicePixels="true" StrokeThickness="1"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <SolidColorBrush x:Key="Button.Static.Background" Color="#FFDDDDDD"/>
        <SolidColorBrush x:Key="Button.Static.Border" Color="#FF707070"/>
        <SolidColorBrush x:Key="Button.MouseOver.Background" Color="#FFBEE6FD"/>
        <SolidColorBrush x:Key="Button.MouseOver.Border" Color="#FF3C7FB1"/>
        <SolidColorBrush x:Key="Button.Pressed.Background" Color="#FFC4E5F6"/>
        <SolidColorBrush x:Key="Button.Pressed.Border" Color="#FF2C628B"/>
        <SolidColorBrush x:Key="Button.Disabled.Background" Color="#FFF4F4F4"/>
        <SolidColorBrush x:Key="Button.Disabled.Border" Color="#FFADB2B5"/>
        <SolidColorBrush x:Key="Button.Disabled.Foreground" Color="#FF838383"/>
        <Style x:Key="RoundedButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}"/>
            <Setter Property="Background" Value="{StaticResource Button.Static.Background}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Button.Static.Border}"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="BorderThickness" Value="0.5"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border" CornerRadius="5" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="true">
                            <ContentPresenter x:Name="contentPresenter" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsDefaulted" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.MouseOver.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.MouseOver.Border}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Pressed.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Pressed.Border}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Disabled.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Disabled.Border}"/>
                                <Setter Property="TextElement.Foreground" TargetName="contentPresenter" Value="{StaticResource Button.Disabled.Foreground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#AA111111" CornerRadius="12" Padding="10" Margin="0,0,0,10">
        <StackPanel Margin="0,0,0,-10">
            <TextBlock Text="Sound Overlay"
                       Foreground="White"
                       TextAlignment="Center"
                       FontSize="18"
                       FontWeight="Light"
                       Margin="0,0,0,10"/>
            <Button Style="{DynamicResource RoundedButtonStyle}" Content="Settings"
                    Click="Settings_Click"
                    Background="Transparent"
                    Foreground="Gray"
                    
                    Width="338"/>

            <!-- beginning -->

            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <!-- Linke Spalte: Soundname -->
                    <ColumnDefinition Width="90" />
                    <!-- Rechte Spalte: Hotkey -->
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Sound 1 -->
                <TextBox x:Name="s1" Grid.Row="0" Grid.Column="0" Margin="10,15,0,2.5" Background="Transparent" BorderThickness="0" Foreground="White" IsReadOnly="True"/>
                <TextBox x:Name="h1" Grid.Row="0" Grid.Column="1" Margin="0,15,0,2.5" Background="Transparent" BorderThickness="0" Foreground="White" TextAlignment="Center"/>

                <!-- Sound 2 -->
                <TextBox x:Name="s2" Grid.Row="1" Grid.Column="0" Margin="10,5,0,2.5" Background="Transparent" BorderThickness="0" Foreground="White" IsReadOnly="True"/>
                <TextBox x:Name="h2" TextAlignment="Center" Grid.Row="1" Grid.Column="1" Margin="0,5,0,2.5" Background="Transparent" BorderThickness="0" Foreground="White"/>

                <!-- Sound 3 -->
                <TextBox x:Name="s3" Grid.Row="2" Grid.Column="0" Margin="10,5,0,2.5" Background="Transparent" BorderThickness="0" Foreground="White" IsReadOnly="True"/>
                <TextBox x:Name="h3"  TextAlignment="Center" Grid.Row="2" Grid.Column="1" Margin="0,5,0,2.5" Background="Transparent" BorderThickness="0" Foreground="White"/>

                <!-- Sound 4 -->
                <TextBox x:Name="s4" Grid.Row="3" Grid.Column="0" Margin="10,5,0,2.5" Background="Transparent" BorderThickness="0" Foreground="White" IsReadOnly="True"/>
                <TextBox x:Name="h4" TextAlignment="Center" Grid.Row="3" Grid.Column="1" Margin="0,5,0,2.5" Background="Transparent" BorderThickness="0" Foreground="White"/>

                <!-- Sound 5 -->
                <TextBox x:Name="s5" Grid.Row="4" Grid.Column="0" Margin="10,5,0,2.5" Background="Transparent" BorderThickness="0" Foreground="White" IsReadOnly="True"/>
                <TextBox x:Name="h5" TextAlignment="Center" Grid.Row="4" Grid.Column="1" Margin="0,5,0,2.5" Background="Transparent" BorderThickness="0" Foreground="White"/>

                <!-- Sound 6 -->
                <TextBox x:Name="s6" Grid.Row="5" Grid.Column="0" Margin="10,5,0,2.5" Background="Transparent" BorderThickness="0" Foreground="White" IsReadOnly="True"/>
                <TextBox x:Name="h6" TextAlignment="Center" Grid.Row="5" Grid.Column="1" Margin="0,5,0,2.5" Background="Transparent" BorderThickness="0" Foreground="White"/>

                <!-- Sound 7 -->
                <TextBox x:Name="s7" Grid.Row="6" Grid.Column="0" Margin="10,5,0,2.5" Background="Transparent" BorderThickness="0" Foreground="White" IsReadOnly="True"/>
                <TextBox x:Name="h7" TextAlignment="Center" Grid.Row="6" Grid.Column="1" Margin="0,5,0,2.5" Background="Transparent" BorderThickness="0" Foreground="White"/>

                <!-- Sound 8 -->
                <TextBox x:Name="s8" Grid.Row="7" Grid.Column="0" Margin="10,5,0,2.5" Background="Transparent" BorderThickness="0" Foreground="White" IsReadOnly="True"/>
                <TextBox x:Name="h8" TextAlignment="Center" Grid.Row="7" Grid.Column="1" Margin="0,5,0,2.5" Background="Transparent" BorderThickness="0" Foreground="White"/>

                <!-- Sound 9 -->
                <TextBox x:Name="s9" Grid.Row="8" Grid.Column="0" Margin="10,5,0,2.5" Background="Transparent" BorderThickness="0" Foreground="White" IsReadOnly="True"/>
                <TextBox x:Name="h9" TextAlignment="Center" Grid.Row="8" Grid.Column="1" Margin="0,5,0,2.5" Background="Transparent" BorderThickness="0" Foreground="White"/>

                <!-- Sound 10 -->
                <TextBox x:Name="s10" Grid.Row="9" Grid.Column="0" Margin="10,5,0,2.5" Background="Transparent" BorderThickness="0" Foreground="White" IsReadOnly="True"/>
                <TextBox x:Name="h10" TextAlignment="Center" Grid.Row="9" Grid.Column="1" Margin="0,5,0,2.5" Background="Transparent" BorderThickness="0" Foreground="White"/>
            </Grid>

            <!-- ending -->


        </StackPanel>
    </Border>
</Window>